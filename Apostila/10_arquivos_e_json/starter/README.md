# Arquivos iniciais — Capítulo 10

Extraia ou copie a pasta completa antes de trabalhar e abra o terminal nela. As atividades alteram os arquivos de dados.

Execute `python verificar_capitulo.py` antes de começar. A mensagem `Ambiente pronto para o Capítulo 10.` confirma apenas que o Python está executando na pasta correta.

| Arquivo | Finalidade |
|---|---|
| `produtos.json` e `catalogo_base.py` | Prática acompanhada. |
| `rascunho.txt` e `modo_incorreto.py` | Oficina sobre modos. |

Se quiser repetir uma atividade, restaure `produtos.json` ou `rascunho.txt` a partir de uma cópia limpa.
